[pytest]
minversion = 7.0
addopts = --strict-markers --maxfail=1 -q --cov=src --cov-report=term-missing --cov-report=html
testpaths = tests
python_files = test_*.py *_test.py
markers =
    integration: marks tests as integration (deselect with -m "not integration")
